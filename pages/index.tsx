import type { NextPage } from "next";
import Button from "../components/Button";

const Home: NextPage = () => {
  return (
    <div style={{ width: 300 }}>
      <Button style={{ width: 700 }} />
    </div>
  );
};

export default Home;
